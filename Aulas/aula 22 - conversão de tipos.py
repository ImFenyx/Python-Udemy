# conversão de tipos, coersão, type conversion, type casting, coercion é o ato de converter um tipo em outro;
# tipos imutáveis e primitivos
# str, int, float, bool

print(1 + 1) #* > 2
print("a" + "b") #* > ab
#? isso acontece pois somar duas strings, é concatenação, ou seja, junção de strings

print(int('1')) # Isso vai converter string em inteiro
print(float("1") + 1) # Isso vai funcionar, sendo 2.0, pois virou float

print(bool( )) # True, pois tem valor, que é o espaço

